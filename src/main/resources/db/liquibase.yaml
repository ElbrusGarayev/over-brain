# production database
url: ${REMOTE_DB_URL}
username: ${REMOTE_DB_USERNAME}
password: ${REMOTE_DB_PASSWORD}

# development database
referenceUrl: ${JDBC_DATABASE_URL}
referenceUsername: ${JDBC_DATABASE_USERNAME}
referencePassword: ${JDBC_DATABASE_PASSWORD}

# for diff/update
changeLogFile: src/main/resources/db/changelog/master.yaml

# for initial changelog creation
outputChangeLogFile: src/main/resources/db/changelog/step0.yaml

# for diff operation
diffChangeLogFile:  src/main/resources/db/changelog/changes/diff.yaml
